import 'package:flutter/material.dart';

class ClassifiedSongListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('分类歌单'),),
    );
  }
}