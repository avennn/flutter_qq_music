import 'package:flutter/material.dart';

class ClassifiedSongListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(title: new Text('分类歌单'),),
    );
  }
}